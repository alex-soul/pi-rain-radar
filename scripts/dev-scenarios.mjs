// Stable scenario IDs are part of the local review workflow. Preserve old cases.
export const scenarios = [
  ['release-behind','About','Three releases behind','The installed version has two patch transitions and one minor transition ahead; all three are pre-releases.','About version has an amber info icon. Tap it for counts, newest version and check time.','Compare Up to date and Unknown version. Reopen About or its bubble after changing the scenario.'],
  ['release-current','About','Up to date','The last successful release check found no newer published version.','About info icon uses normal text colour; the bubble says you are up to date.','Review in both themes. No extra line is added beside the version.'],
  ['release-unknown','About','Unknown version check','No successful version check is available.','Neutral info icon and honest unavailable wording; radar health is unaffected.','Compare Cached release result.'],
  ['release-stale','About','Cached release result','A newer-release result was saved two days ago; the latest refresh failed.','Amber icon; bubble dates the older result and reports the failed check.','It must not claim this is a fresh result.'],
  ['release-partial','About','Partial release history','The bounded release history is incomplete, with three known newer versions.','At least three releases behind; no precise version-change breakdown.','Compare Three releases behind for the complete result.'],
  ['healthy','Playback','Healthy display','All sources report usable data. The forecast includes a short band of rain.','Both handles green; readings and forecast visible.','Use this to verify recovery after any failure.'],
  ['mixed','Playback','Gaps in history','Remove isolated Main captures, five consecutive Overview captures and one capture from both maps.','Amber timeline gaps; acquisition handles stay green because current sources are healthy.','Compare with Recovered history. Historical gaps are not an outage.'],
  ['one','Playback','One capture','Leave only the newest generated capture in the selected window.','One still image; playback waits because there is nothing to animate.','Select Recovered history to check automatic playback resumes.'],
  ['empty','Playback','No captures','Remove all captures from the selected playback window, while synthetic acquisition status remains healthy.','No playable sequence. This tests empty playback, independently of source health.','Select Recovered history to restore playback.'],
  ['recovered','Playback','Recovered history','Restore the complete generated history after a missing-frame case.','Gaps clear and available playback resumes unless manually paused.','Compare against Mixed history, One capture or No captures.'],
  ['gap-grace','Stats','Gap recovered within grace','Overview briefly had a visible gap after Main arrived early. It recovered before the ten-minute grace deadline.','Stats: Overview has 1 gap seen and 0 late arrivals. No frames missing now; timeline green.','Open Stats for nerds. Historical incident counts do not change health colours.'],
  ['late-recovered','Stats','Late arrivals recovered','Two Main gaps and one Overview gap were seen. One Main observation arrived after its grace deadline; all have now recovered.','Stats: Main 2 gaps seen / 1 late arrival; Overview 1 gap seen / 0 late arrivals. Missing is zero.','Change Live window or select Archive. Counts belong to observation times, not arrival times.'],
  ['gap-outstanding','Stats','Gap still missing','One Overview observation is still missing after becoming visible. It has not arrived, so it cannot be a late arrival yet.','Stats: Overview 1 missing / 1 gap seen / 0 late arrivals. Main remains complete.','Compare Gap recovered within grace. Available and missing describe now; gaps seen preserves history.'],
  ['tracking-partial','Stats','Partial tracking history','Only the newest four observation slots have incident evidence. Older images came from an untracked archive.','Stats reports partial tracking. The known Main late arrival is counted; unknown history is never presented as a clean zero.','Healthy display uses the legacy image-only fixture and shows Untracked. These are synthetic display examples; automated archive tests exercise persistence.'],
  ['source-error','Radar','Refresh delayed','Both radar providers report a failed refresh but retain fresh observations.','Bottom handle amber; cached radar continues playing. Top handle green.','Select Healthy display to clear the warning.'],
  ['main-stale','Radar','Main radar stale','Main observation is 30 minutes old, even though its provider reports only a warning.','Bottom handle red; Main source identified in Settings. Current weather handle remains green.','Select Healthy display to check recovery.'],
  ['overview-stale','Radar','Overview radar stale','Overview observation is 30 minutes old; Main remains fresh.','Bottom handle red because the worst radar source wins. Current weather handle remains green.','Select Healthy display to check recovery.'],
  ['startup','Radar','First acquisition','Sources are still fetching their first usable observation.','Both handles amber; weather readings are dashes and forecast baseline amber.','Generated radar history remains available in this fixture; this isolates startup status.'],
  ['first-failure','Radar','First acquisition failed','The first radar and weather acquisitions have completed without usable data.','Both handles red; readings are dashes and forecast baseline red.','Generated playback remains available; no real acquisition is performed.'],
  ['weather-first','Weather','Current weather · first failure','Current weather fails once, with fresh retained readings. Forecast remains healthy.','Top handle amber; retained readings amber; forecast still visible.','Next try Current weather · repeated failure to cross the retention boundary.'],
  ['weather-second','Weather','Current weather · repeated failure','Current weather fails twice; forecast still succeeds.','Top handle red; current readings become dashes while forecast stays healthy.','Select Healthy display to restore readings.'],
  ['forecast-failed','Weather','Minute forecast · unavailable','Forecast refresh fails immediately; current weather remains usable.','Current weather handle stays green; readings remain; old forecast bars become a thin red baseline.','Compare with Current weather · repeated failure to identify the affected component.'],
  ['forecast-partial','Weather','Minute forecast · missing minutes','Remove five forecast minutes from an otherwise usable forecast.','Current weather handle stays green; only the missing minutes show amber baseline segments.','Select Healthy display to restore complete data.'],
  ['current-expired','Weather','Current weather · expired','Only the current-weather observation is at least 30 minutes old. Forecast remains fresh.','Current weather handle red and current readings are dashes; forecast stays usable. Log records Current weather unavailable.','Compare with Minute forecast · expired, then Healthy display to verify independent recovery.'],
  ['forecast-expired','Weather','Minute forecast · expired','Only the forecast acquisition is at least 30 minutes old. Current weather remains fresh.','Forecast baseline red; current weather handle green and readings retained. Log records Minute forecast unavailable.','Compare with Current weather · expired, then Healthy display to verify independent recovery.'],
  ['weather-expired','Weather','Weather · both endpoints expired','Current weather and the forecast acquisition are at least 30 minutes old. This preserves the original combined expiry case.','Current weather handle red; current readings are dashes and forecast baseline red. Separate Current weather and Minute forecast errors appear in the log.','Radar remains healthy. Select Healthy display to recover both feeds.'],
  ['no-key','Weather','Weather · not configured','No weather key is configured in the synthetic response.','Current weather handle and forecast baseline neutral; no error for an optional service.','No real key is needed. Select Healthy display to restore synthetic weather.'],
  ['unreachable','Connection','Backend unavailable','Return HTTP 503 for every application API, including status, Settings, Log and Archive, plus the health endpoint. The scenario console remains available to restore service.','Both handles turn red after the next poll. Opening Settings cannot complete; an already-open Log shows cached and browser-local events. Already loaded radar can keep playing.','Select Healthy display and leave the radar page open to check recovery. UI and image files still load: this tests backend loss, not a complete network failure or Pi shutdown.'],
].map(([id,group,title,description,expected,review])=>{
  const current=['weather-first','weather-second','current-expired'].includes(id);
  const forecast=['forecast-failed','forecast-partial','forecast-expired'].includes(id);
  const endpoint=current?'current':forecast?'timeline/1min':group==='Weather'?'current + timeline/1min':null;
  const detail=endpoint ? description+' OpenWeather endpoint'+(current||forecast?': ':'s: ')+endpoint+' (One Call 4.0).' : description;
  return {id,group,title,description:detail,expected,review};
});

const escape = value => value.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');
export function controlsPage(active) {
  const selected=scenarios.find(s=>s.id===active);
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Rain Radar · Scenario studio</title><link rel="stylesheet" href="/__dev/style.css"><script src="/__dev/controls.js" defer></script></head><body>
  <header><a class="brand" href="/__dev"><span class="radar-icon">◉</span> PI RAIN RADAR <span>/ development</span></a><a class="open" href="/__embed" target="embed-preview">Review embeds ↗</a><a class="open" href="/" target="radar-preview">Open radar preview ↗</a></header>
  <main><div class="intro"><span class="eyebrow">LOCAL SIMULATION · NO PROVIDER CALLS</span><h1>A little weather.<br>A lot of possibilities.</h1><p>Explore failures, recovery and the spaces in between.<br>Same radar UI. Repeatable synthetic conditions.</p></div>
  <div class="workspace"><section class="library" aria-label="Scenario library"><div class="section-title"><h2>Scenario library</h2><span>${scenarios.length} cases</span></div>${[...new Set(scenarios.map(s=>s.group))].map(group=>`<section class="group"><h3>${group}</h3><div class="cards">${scenarios.filter(s=>s.group===group).map(s=>`<button data-scenario="${s.id}" aria-pressed="${s.id===active}"><span>${s.title}</span><small>${s.id}</small></button>`).join('')}</div></section>`).join('')}</section>
  <aside><section class="detail" aria-label="Selected scenario"><div class="live"><i></i><span id="selection-status" role="status">Active simulation</span></div><p class="eyebrow" id="scenario-group">${selected.group}</p><h2 id="scenario-title">${selected.title}</h2><p id="scenario-description">${selected.description}</p><div class="expect"><h3>What you should see</h3><p id="scenario-expected">${selected.expected}</p></div><h3>Try this next</h3><p id="scenario-review">${selected.review}</p><p class="timing">The radar adopts changes on its next status poll, within 15 seconds. Keep it open to watch transitions, or refresh for an immediate check.</p><p id="request-error" role="alert" hidden></p></section><p class="footnote">Only this local preview changes. All open preview windows share the selected scenario. Your Pi and Home Assistant are untouched.</p></aside></div>
  <footer>Built for repeatable reviews <span>Experimental developer tooling · synthetic data</span></footer></main>
  <template id="scenario-data">${escape(JSON.stringify(scenarios))}</template></body></html>`;
}
